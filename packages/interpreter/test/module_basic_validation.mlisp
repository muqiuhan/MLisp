;; Basic module system validation test

;; Test 1: Simple module with variable
(module simple (export x)
  (define x 42))

;; Verify module is bound
simple

;; Test 2: Import and use
(import simple)
x

;; Test 3: Module with function
(module math (export add)
  (define add (lambda (x y) (+ x y))))

(import math)
(add 5 3)
