;; Simple module test

;; Define a simple math module
(module math (export add)
  (:= add (=> (x y) (+ x y))))

;; Import all symbols from math module
(import math)

;; Test imported function
(add 5 3)
